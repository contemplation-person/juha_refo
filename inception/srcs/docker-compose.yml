version: '3'

services:
  # mariadb:
  #   volumes:
  #     - ./requirements/mariadb/mariadb-db:/db
  #   build: ./requirements/mariadb
  #   networks:
  #     - db 
  #   command: mariadbd
  #   restart: always

  wordpress:
    volumes:
      - word:/wp/
    build:
      context: ./requirements/wordpress/
    networks:
      - inception
      # - db
    # command: ./mkdir.sh && php-fpm7.4 -F
    command: ./mkdir.sh 
    # depends_on:
      # - mariadb
    restart: always

  nginx:
    volumes:
      - word:/wp/
    build: ./requirements/nginx
    ports:
      - "443:443"
    networks:
      - inception
    command: nginx -g "daemon off;"
    depends_on:
      - wordpress
    restart: always

# TODO how to do?
volumes:
  word:
    driver: local
    driver_opts:
      o: bind
      type: none
      #device: /Users/juha/goinfre/wp-data
      device: ./requirements/wordpress/data/

networks:
  inception:
  # db:
